package redis_test

import (
	"testing"
)

func TestCache(t *testing.T) {
	//c := redis.NewCache()

	// err := c.Save(&models.Recipe{})
	// assert.NoError(t, err)
	// res, err := c.Get("test")
	// assert.NoError(t, err)
	// assert.Equal(t, "test", res.Title)
}
